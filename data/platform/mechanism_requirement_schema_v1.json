{
  "description": "Battery mechanism audit metadata schema. This is a compact tracked contract, not a row-level payload.",
  "required_fields": [
    "requirement_id",
    "mechanism_id",
    "requirement_type",
    "required_concept",
    "required_evidence",
    "prohibited_substitute"
  ],
  "schema_name": "mechanism_requirement_schema_v1",
  "schema_version": "2.3.3",
  "security_rules": [
    "No raw cycle series, cell-level payload list, credentials, or local absolute paths.",
    "No parameter fitting, solver execution, predictive model execution, or hidden-state inference.",
    "Missing evidence must be represented explicitly and must not be silently satisfied by defaults.",
    "Mechanism readiness must not be promoted to mechanism confirmation."
  ],
  "status": "accepted_for_v2_3"
}
