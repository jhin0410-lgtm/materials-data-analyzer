# Release Readiness Audit Scope

The authoritative audit entry point is
`docs/CROSS_REPOSITORY_RELEASE_READINESS.md`.

This short file exists only to make the release-readiness scope explicit in
path-based review and CI triggers. It does not define a separate release policy.
